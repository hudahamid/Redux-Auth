intialState= {
    loading:false,
    currentUser:null,
    error:null,
}

const userReducer=(state=intialState,action) =>{
    switch(action.type){
        default:
        return state
    }
}

export default userReducer;

